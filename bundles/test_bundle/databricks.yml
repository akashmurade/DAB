# This is a Databricks asset bundle definition for test_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: test_bundle
  uuid: 71668e0f-a0a1-4cd0-9967-f2b6640309e4

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-c06ac1d5-da13.cloud.databricks.com
    variables:
      warehouse_id: 244ec9db44f70240
      catalog: experiment_catalog
      schema: bundle_schema

  prod:
    mode: production
    workspace:
      host: https://dbc-c06ac1d5-da13.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/akash.n.murade@v4c.ai to make sure we only have a single copy.
      root_path: /Workspace/Users/akash.n.murade@v4c.ai/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: 244ec9db44f70240
      catalog: experiment_catalog
      schema: bundle_schema
    permissions:
      - user_name: akash.n.murade@v4c.ai
        level: CAN_MANAGE
